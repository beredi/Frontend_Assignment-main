import styled from "styled-components";

export const FormPhoneInputStyled = styled.div`
  position: relative;
  .custom-dropdown {
    position: absolute;
    z-index: 100;
    top: 36px;
    left: 17px;
    border: 0;
  }
  div input {
    padding-left: 55px;
  }
`;
